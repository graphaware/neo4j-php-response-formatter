{
    "statements": [
        {
            "statement": "MATCH (n) RETURN count(n) as x",
            "params": [],
            "resultDataContents": [
                "row",
                "graph",
                "rest"
            ]
        },
        {
            "statement": "CREATE (n:User)-[r:KNOWS]->(b:User {id:234}) RETURN *",
            "params": [],
            "resultDataContents": [
                "row",
                "graph",
                "rest"
            ]
        },
        {
            "statement": "MATCH (n) OPTIONAL MATCH (n)-[r]-() RETURN n, type(r), endNode(r)",
            "params": [],
            "resultDataContents": [
                "row",
                "graph",
                "rest"
            ]
        },
        {
            "statement": "MATCH (n) OPTIONAL MATCH (n)-[r]-() RETURN n, collect(r) as collection",
            "params": [],
            "resultDataContents": [
                "row",
                "graph",
                "rest"
            ]
        }
    ]
}